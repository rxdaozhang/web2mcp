{
  "server": {
    "name": "email-webapp-mcp-server",
    "version": "0.1.0"
  },
  "tools": [
    {
      "name": "list_emails",
      "description": "List emails with optional text search and starred-only filter",
      "input_schema": {
        "type": "object",
        "properties": {
          "query": { "type": "string", "description": "Search query to filter emails by subject or body" },
          "starred": { "type": "boolean", "description": "If true, return only starred emails" }
        },
        "required": [],
        "additionalProperties": false
      }
    },
    {
      "name": "send_email",
      "description": "Compose and send a new email",
      "input_schema": {
        "type": "object",
        "properties": {
          "subject": { "type": "string", "description": "Email subject" },
          "body": { "type": "string", "description": "Email body content" }
        },
        "required": ["subject", "body"],
        "additionalProperties": false
      }
    }
  ]
}